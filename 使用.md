# 山东麻将 AI 前端使用说明

## 1. 环境要求
- Node.js 20+（本地已验证 `v24.13.0`）
- npm 10+（本地已验证 `11.6.2`）

## 2. 快速启动
```powershell
cd "S:\山东麻将AI设计\app"
cmd /c npm install
cmd /c npm run dev
```

然后在浏览器打开终端显示的地址（默认通常是 `http://localhost:5173`）。

## 3. `npm install` 容易超时时的建议
先执行下面这组更稳的安装命令：

```powershell
cd "S:\山东麻将AI设计\app"
cmd /c npm config set registry https://registry.npmmirror.com
cmd /c npm config set fetch-retries 5
cmd /c npm config set fetch-retry-mintimeout 20000
cmd /c npm config set fetch-retry-maxtimeout 120000
cmd /c npm install --prefer-offline --no-audit --progress=false
```

如果你后续想切回官方源：

```powershell
cd "S:\山东麻将AI设计\app"
cmd /c npm config set registry https://registry.npmjs.org
```

## 4. 常用命令
```powershell
cd "S:\山东麻将AI设计\app"
cmd /c npm run build
cmd /c npm run preview
```

停止服务：`Ctrl + C`

## 5. 当前可用性检查结果（2026-02-20）
- `cmd /c npm install --prefer-offline --no-audit --progress=false`：通过（up to date）。
- `cmd /c npm run build`：通过，成功产出 `app/dist`。
- `cmd /c npm run dev` / `cmd /c npm run preview`：在当前受限沙箱里报 `spawn EPERM`，属于运行环境限制；在本机正常终端（非受限沙箱）可按上面命令启动。

 在 PowerShell 里直接按这个清单执行（最省事，先直接上线）：

  cd "S:\山东麻将AI设计\app"
  npm install
  npm run build
  npm install -g vercel
  vercel login
  vercel --prod
  执行完最后一条会输出线上地址（https://xxx.vercel.app），安卓 Chrome 直接打开即可。

  如果你还要“代码一改自动更新线上站点”，再执行这组（GitHub 自动部署）：

  @"
  node_modules/
  app/node_modules/
  app/dist/
  .env
  .env.*
  !.env.example
  "@ | Set-Content .gitignore -Encoding UTF8

  git init
  git branch -M main
  git add .
  git commit -m "init: deploy mahjong app"

  gh auth login
  gh repo create <你的GitHub用户名>/<仓库名> --public --source . --remote origin --push

  然后去 Vercel 网页导入这个 GitHub 仓库，Root Directory 选 app，以后 git push 就会自动更新网址。

🔗  Linked to jyl14027-8215s-projects/majiang1 (created .vercel and added it to .gitignore)
🔍  Inspect: https://vercel.com/jyl14027-8215s-projects/majiang1/4vEkrB5MnictC1bcbQzX5dAq9v3g [4s]
✅  Production: https://majiang1-auzg5dc46-jyl14027-8215s-projects.vercel.app [36s]
🔗  Aliased: https://majiang1-sandy.vercel.app [36s]
